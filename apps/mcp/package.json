{
  "name": "@brewdial/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "brewdial-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "pnpm --filter @brewdial/shared build && tsc",
    "check": "pnpm --filter @brewdial/shared build && tsc --noEmit",
    "test": "pnpm --filter @brewdial/shared build && vitest run",
    "lint": "echo '@brewdial/mcp: no lint configured' && exit 0",
    "mcp": "node dist/index.js",
    "mcp:dev": "pnpm --filter @brewdial/shared build && tsx src/index.ts",
    "smoke": "pnpm --filter @brewdial/shared build && tsx scripts/smoke-test.ts"
  },
  "dependencies": {
    "@brewdial/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.15.0",
    "@sentry/node": "^10.59.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
